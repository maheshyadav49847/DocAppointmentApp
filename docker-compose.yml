version: '3.8'

services:
  frontend:
    image: ghcr.io/${GITHUB_REPOSITORY}/frontend:${IMAGE_TAG:-latest}
    restart: always
    ports:
      - "${FRONTEND_PORT:-80}:80"
    environment:
      - NODE_ENV=production

  backend:
    image: ghcr.io/${GITHUB_REPOSITORY}/backend:${IMAGE_TAG:-latest}
    restart: always
    network_mode: "host"
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNET_ENV:-Production}
      - ASPNETCORE_URLS=http://+:${BACKEND_PORT:-8080}
      - AllowedOrigins=http://${VPS_IP}
      # Point this to your EXISTING database on the VPS. 
      - ConnectionStrings__DefaultConnection=${DB_CONNECTION_STRING:-Host=localhost;Database=DocAppointmentDb_Prod;Username=postgres;Password=sa}

