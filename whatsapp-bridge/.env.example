# WhatsApp Bridge Configuration

# Port for the bridge server
PORT=3101

# Backend webhook endpoint - where incoming messages are forwarded
BACKEND_WEBHOOK_URL=http://localhost:5000/api/whatsapp/webhook

# API Key for authenticating requests to the backend
BRIDGE_API_KEY=codex-secret-123
